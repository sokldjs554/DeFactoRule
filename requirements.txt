# 실행에 필요한 것 전부. Python 3.9 이상.
pymupdf>=1.24        # PDF 텍스트·구조 추출
anthropic>=0.40      # LLM 분류기 (app/agents/classifier.py)

# 서비스 계층. 파이프라인만 돌릴 것이라면 없어도 된다 —
# app/api 를 임포트할 때만 필요하고, 관련 테스트는 자동으로 건너뛴다.
fastapi>=0.110       # API
pydantic>=2.0        # 경계에서의 형식 강제 (명세 §9 의 schema validation)
uvicorn>=0.27        # 개발 서버
numpy>=1.24
